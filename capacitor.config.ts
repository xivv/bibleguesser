import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bibleguesser',
  appName: 'bibleguesser',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
